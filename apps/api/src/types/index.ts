/**
 * @fileoverview Barrel export for API type definitions.
 *
 * @module @plpg/api/types
 * @description Central export point for all API-specific types.
 */

export type { AuthenticatedUser, AuthenticatedUserSubscription } from './express';
